import { Fragment } from 'react';
import { FaClipboardList, FaStar, FaTag, FaReceipt } from 'react-icons/fa';

const ProductDetails = ({items}) => {
    const {brand, category, description, stars, price, off} = items;
    console.log(items)

    return ( 
        <Fragment>

        </Fragment>
     );
}
 
export default ProductDetails;